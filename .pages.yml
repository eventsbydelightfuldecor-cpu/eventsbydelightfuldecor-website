media:
  input: images
  output: /images

content:
  - name: homepage
    label: Home
    type: file
    path: index.html
    format: raw
    fields:
      - { name: body, label: Page Content, type: code, options: { language: html } }

  - name: services
    label: Services
    type: file
    path: pages/services.html
    format: raw
    fields:
      - { name: body, label: Page Content, type: code, options: { language: html } }

  - name: gallery
    label: Gallery
    type: file
    path: pages/gallery.html
    format: raw
    fields:
      - { name: body, label: Page Content, type: code, options: { language: html } }

  - name: testimonials
    label: Testimonials
    type: file
    path: pages/testimonials.html
    format: raw
    fields:
      - { name: body, label: Page Content, type: code, options: { language: html } }

  - name: contact
    label: Contact
    type: file
    path: pages/contact.html
    format: raw
    fields:
      - { name: body, label: Page Content, type: code, options: { language: html } }

  - name: about
    label: About
    type: file
    path: pages/about.html
    format: raw
    fields:
      - { name: body, label: Page Content, type: code, options: { language: html } }

  - name: give-a-gift
    label: Give-A-Gift
    type: file
    path: pages/give-a-gift.html
    format: raw
    fields:
      - { name: body, label: Page Content, type: code, options: { language: html } }

  - name: faq
    label: FAQ
    type: file
    path: pages/faq.html
    format: raw
    fields:
      - { name: body, label: Page Content, type: code, options: { language: html } }
